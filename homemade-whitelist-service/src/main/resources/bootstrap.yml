spring:
  application:
    name: homemade-whitelist-service
server:
  port: ${PORT:8082}
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8081/eureka}
  instance:
    preferIpAddress: true