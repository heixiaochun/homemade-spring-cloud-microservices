spring:
  application:
    name: homemade-eureka-service
server:
  port: ${PORT:8081}
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://localhost:${server.port}/eureka/